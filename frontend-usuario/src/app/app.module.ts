mport { HttpClientModule } from '@angular/common/http';

@NgModule({
  declarations: [
    // Componentes aqui
  ],
  imports: [
    HttpClientModule,
    // Outros módulos
  ],
  providers: [],
  bootstrap: [AppComponent]
})
export class AppModule { }